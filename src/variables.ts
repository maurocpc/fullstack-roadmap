// 1. String (Text)
const city: string = "São Paulo"; 

// 2. Number (Integers or Decimals)
let temperature: number = 25.5; 

// 3. Boolean (True or False - like a light switch)
const isRaining: boolean = false;

// 4. Array (A list of items)
let tools: string[] = ["VS Code", "Git", "TypeScript"];

console.log(`In ${city}, it is ${temperature} degrees.`);